# Century Metadata Format Specification

## File Format

### Reader Requirements

A reader:
  - MUST fail parsing if the length is wrong.

### Writer Requirements

A writer:
  - MUST fail parsing if the length is wrong.
